  <%= form_for(@group) do |f| %>
  <label>Join a Group!</label><br>
  <br>
  <%=f.collection_select :name, Group.all, :name,:name %>
  <%=f.collection_select :id, User.all, :id,:name %>
  <%= f.submit "Join Group" %>
<% end %>
